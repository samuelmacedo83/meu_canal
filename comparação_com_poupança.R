aporte_inicial <- 5000
aporte_mensal <- 300
qtd_meses <- 12
taxa <- 0.0024


simulacao <- function(aporte_inicial, aporte_mensal, qtd_meses, taxa) {
  x <- aporte_inicial
  montante_mensal
}

capital <- aporte_mensal * 1:qtd_meses
juros <- (1 + taxa)^(qtd_meses:0)
sum(capital, juros)


aporte_inicial * (1 + taxa)^(qtd_meses) + aporte_mensal + sum(aporte_mensal * (1 + taxa)^(qtd_meses:1))  

